[
  {
    "name": "frappe_ping",
    "description": "Check connectivity to the configured Frappe/ERPNext site."
  },
  {
    "name": "frappe_get_doc",
    "description": "Retrieve a single document (doctype record) by its doctype and document name.",
    "arguments": [
      {
        "name": "doctype",
        "type": "string",
        "desc": "The DocType (e.g. 'Sales Invoice', 'Customer', 'Item')."
      },
      {
        "name": "name",
        "type": "string",
        "desc": "The document name/ID (e.g. 'SINV-00001', 'CUST-001')."
      }
    ]
  },
  {
    "name": "frappe_search_docs",
    "description": "Search or list documents of a given DocType with optional filters, field selection, pagination, and ordering.",
    "arguments": [
      {
        "name": "doctype",
        "type": "string",
        "desc": "The DocType to search."
      },
      {
        "name": "filters",
        "type": "array",
        "desc": "Optional list of filters, each as [field, operator, value], e.g. [['status','=','Open']]."
      },
      {
        "name": "fields",
        "type": "array",
        "desc": "Optional list of field names to return (e.g. ['name', 'status', 'grand_total'])."
      },
      {
        "name": "limit",
        "type": "integer",
        "desc": "Maximum number of results (default 20)."
      },
      {
        "name": "order_by",
        "type": "string",
        "desc": "Field to sort by (e.g. 'creation desc')."
      }
    ]
  },
  {
    "name": "frappe_create_doc",
    "description": "Create a new document of the given DocType with the provided field values.",
    "arguments": [
      {
        "name": "doctype",
        "type": "string",
        "desc": "The DocType to create."
      },
      {
        "name": "data",
        "type": "object",
        "desc": "Key-value pairs of fields and their values."
      }
    ]
  },
  {
    "name": "frappe_update_doc",
    "description": "Update an existing document identified by doctype and name with the provided field values.",
    "arguments": [
      {
        "name": "doctype",
        "type": "string",
        "desc": "The DocType of the document."
      },
      {
        "name": "name",
        "type": "string",
        "desc": "The document name/ID."
      },
      {
        "name": "data",
        "type": "object",
        "desc": "Key-value pairs of fields to update."
      }
    ]
  },
  {
    "name": "frappe_delete_doc",
    "description": "Delete a document by its doctype and name.",
    "arguments": [
      {
        "name": "doctype",
        "type": "string",
        "desc": "The DocType of the document to delete."
      },
      {
        "name": "name",
        "type": "string",
        "desc": "The document name/ID to delete."
      }
    ]
  },
  {
    "name": "frappe_run_method",
    "description": "Call a whitelisted server-side Python method on the Frappe site (e.g. 'frappe.auth.get_logged_user').",
    "arguments": [
      {
        "name": "method",
        "type": "string",
        "desc": "Dotted path to the method (e.g. 'frappe.auth.get_logged_user' or 'erpnext.stock.utils.get_stock_balance')."
      },
      {
        "name": "kwargs",
        "type": "object",
        "desc": "Keyword arguments to pass to the method."
      }
    ]
  }
]
